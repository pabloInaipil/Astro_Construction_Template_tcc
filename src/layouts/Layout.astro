---
// src/layouts/Layout.astro
// Layout base — MGI Estructuras Metálicas
// SEO completo: meta tags, Open Graph, JSON-LD, canonical

import '../styles/global.css';
import { siteConfig } from '@config/site';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  ogType?: string;
  canonicalUrl?: string;
  noindex?: boolean;
}

const {
  title = `${siteConfig.name} — ${siteConfig.tagline}`,
  description = siteConfig.description,
  ogImage = '/og-image.jpg',
  ogType = 'website',
  canonicalUrl = Astro.url.href,
  noindex = false,
} = Astro.props;

const { siteUrl } = siteConfig;
const fullOgImage = ogImage.startsWith('http') ? ogImage : `${siteUrl}${ogImage}`;

// Redes sociales (filtrar vacías)
const activeSocials = Object.entries(siteConfig.social)
  .filter(([_key, url]) => url)
  .map(([_key, url]) => url);

// JSON-LD — LocalBusiness
const jsonLd: Record<string, unknown> = {
  '@context': 'https://schema.org',
  '@type': 'LocalBusiness',
  name: siteConfig.name,
  description: siteConfig.description,
  url: siteUrl,
  logo: `${siteUrl}/logo.png`,
  image: fullOgImage,
  telephone: siteConfig.phone,
  email: siteConfig.email,
  address: {
    '@type': 'PostalAddress',
    ...(siteConfig.address.street ? { streetAddress: siteConfig.address.street } : {}),
    addressLocality: siteConfig.address.city,
    addressRegion: siteConfig.address.region,
    addressCountry: siteConfig.address.country,
  },
  areaServed: {
    '@type': 'Country',
    name: 'Chile',
  },
  ...(activeSocials.length > 0 ? { sameAs: activeSocials } : {}),
  priceRange: '$$',
  openingHoursSpecification: {
    '@type': 'OpeningHoursSpecification',
    dayOfWeek: ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'],
    opens: '08:00',
    closes: '18:00',
  },
};

// WhatsApp URL
const waUrl = `https://wa.me/${siteConfig.whatsapp}?text=${encodeURIComponent(siteConfig.whatsappMessage)}`;
---

<!DOCTYPE html>
<html lang="es" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="theme-color" content="#1E2D4D" />

    <!-- Google Tag Manager (condicional) -->
    {siteConfig.gtmId && (
      <script is:inline define:vars={{ gtmId: siteConfig.gtmId }}>
        (function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
        new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
        j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
        'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
        })(window,document,'script','dataLayer',gtmId);
      </script>
    )}

    <!-- SEO -->
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />
    {noindex && <meta name="robots" content="noindex, nofollow" />}
    <meta name="author" content={siteConfig.name} />
    <meta name="generator" content={Astro.generator} />

    <!-- Open Graph -->
    <meta property="og:type" content={ogType} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={fullOgImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:site_name" content={siteConfig.name} />
    <meta property="og:locale" content="es_CL" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={fullOgImage} />

    <!-- JSON-LD -->
    <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />

    <!-- Preload fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Playfair+Display:wght@400;500;700&family=Plus+Jakarta+Sans:wght@600;700;800&display=swap"
      rel="stylesheet"
    />

    <!-- Theme initialization (prevent FOUC) -->
    <script is:inline>
      (function () {
        const stored = localStorage.getItem('theme');
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        if (stored === 'dark' || (!stored && prefersDark)) {
          document.documentElement.classList.add('dark');
        }
      })();
    </script>
  </head>

  <body class="min-h-screen flex flex-col overflow-x-hidden">
    <!-- Google Tag Manager (noscript) — condicional -->
    {siteConfig.gtmId && (
      <noscript>
        <iframe src={`https://www.googletagmanager.com/ns.html?id=${siteConfig.gtmId}`}
          height="0" width="0" style="display:none;visibility:hidden"></iframe>
      </noscript>
    )}

    <!-- Skip to content (accesibilidad) -->
    <a
      href="#main-content"
      class="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[100]
             focus:px-4 focus:py-2 focus:bg-primary-600 focus:text-white focus:rounded-lg"
    >
      Saltar al contenido principal
    </a>

    <!-- ========== HEADER / NAV ========== -->
    <header
      id="site-header"
      class="fixed top-0 inset-x-0 z-50 transition-all duration-300"
      style="background: transparent;"
    >
      <nav class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8" aria-label="Navegación principal">
        <div class="flex h-16 items-center justify-between lg:h-20">
          <!-- Logo -->
          <a href="/" class="flex items-center gap-3 group" aria-label={`${siteConfig.shortName} — Inicio`}>
            <img
              src="/logo.png"
              alt={siteConfig.name}
              width="40"
              height="40"
              class="h-9 w-9 lg:h-10 lg:w-10 object-contain rounded-lg transition-transform duration-300 group-hover:scale-105"
            />
            <div class="hidden sm:block">
              <span class="logo-name block text-lg font-heading font-bold transition-colors duration-300" style="color: var(--text-heading);">
                {siteConfig.shortName}
              </span>
              <span class="logo-sub block text-[10px] uppercase tracking-[0.2em] font-medium transition-colors duration-300" style="color: var(--text-muted);">
                Estructuras Metálicas
              </span>
            </div>
          </a>

          <!-- Desktop Nav -->
          <div class="hidden lg:flex items-center gap-8">
            <a href="#servicios" class="nav-link">Servicios</a>
            <a href="#proyectos" class="nav-link">Proyectos</a>
            <a href="#nosotros" class="nav-link">Nosotros</a>
            <a href="#contacto" class="nav-link">Contacto</a>

            <!-- Theme toggle -->
            <button
              id="theme-toggle"
              type="button"
              class="p-2 rounded-lg transition-colors duration-200 hover:bg-surface-200 dark:hover:bg-surface-800"
              aria-label="Cambiar tema claro/oscuro"
            >
              <svg class="theme-toggle-icon hidden dark:block w-5 h-5" style="color: var(--color-accent-400);" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 3v1m0 16v1m8.66-13.66l-.71.71M4.05 19.95l-.71.71M21 12h-1M4 12H3m16.66 7.66l-.71-.71M4.05 4.05l-.71-.71M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
              </svg>
              <svg class="theme-toggle-icon block dark:hidden w-5 h-5" style="color: var(--color-steel-600);" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
              </svg>
            </button>

            <!-- CTA -->
            <a href="#contacto" class="btn-primary">
              Cotizar Ahora
            </a>
          </div>

          <!-- Mobile: theme toggle + hamburger -->
          <div class="flex items-center gap-2 lg:hidden">
            <button
              id="theme-toggle-mobile"
              type="button"
              class="p-2 rounded-lg"
              aria-label="Cambiar tema claro/oscuro"
            >
              <svg class="theme-toggle-icon hidden dark:block w-5 h-5" style="color: var(--color-accent-400);" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 3v1m0 16v1m8.66-13.66l-.71.71M4.05 19.95l-.71.71M21 12h-1M4 12H3m16.66 7.66l-.71-.71M4.05 4.05l-.71-.71M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
              </svg>
              <svg class="theme-toggle-icon block dark:hidden w-5 h-5" style="color: var(--color-steel-600);" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
              </svg>
            </button>
            <button
              id="mobile-menu-btn"
              type="button"
              class="p-2 rounded-lg"
              aria-label="Abrir menú"
              aria-expanded="false"
            >
              <svg class="burger-icon w-6 h-6 transition-colors duration-300" style="color: var(--text-heading);" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path id="menu-icon-open" stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
                <path id="menu-icon-close" class="hidden" stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
              </svg>
            </button>
          </div>
        </div>

        <!-- Mobile Menu -->
        <div
          id="mobile-menu"
          class="hidden lg:hidden overflow-hidden transition-all duration-300 max-h-0"
        >
          <div class="py-4 space-y-1 border-t" style="border-color: var(--border-default);">
            <a href="#servicios" class="mobile-nav-link">Servicios</a>
            <a href="#proyectos" class="mobile-nav-link">Proyectos</a>
            <a href="#nosotros" class="mobile-nav-link">Nosotros</a>
            <a href="#contacto" class="mobile-nav-link">Contacto</a>
            <div class="pt-3">
              <a href="#contacto" class="btn-primary w-full text-center">
                Cotizar Ahora
              </a>
            </div>
          </div>
        </div>
      </nav>
    </header>

    <!-- ========== MAIN ========== -->
    <main id="main-content" class="flex-1">
      <slot />
    </main>

    <!-- ========== FOOTER ========== -->
    <footer style="background-color: var(--color-primary-950);" class="text-surface-300">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-12 lg:py-16">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-10">
          <!-- Brand -->
          <div class="lg:col-span-1">
            <img src="/logo_footer.png" alt={siteConfig.shortName} width="48" height="48" class="h-12 w-auto mb-4 brightness-110" />
            <p class="text-sm text-surface-300 leading-relaxed">
              Fabricación e instalación de estructuras metálicas:
              galpones, casas, rejas y portones con calidad garantizada.
            </p>
          </div>

          <!-- Links — Servicios -->
          <div>
            <h3 class="text-white font-heading font-semibold text-sm uppercase tracking-wider mb-4">
              Servicios
            </h3>
            <ul class="space-y-2 text-sm">
              {siteConfig.services.map((svc) => (
                <li>
                  <a href="#servicios" class="text-surface-300 hover:text-accent-400 transition-colors">
                    {svc.title}
                  </a>
                </li>
              ))}
            </ul>
          </div>

          <!-- Contacto -->
          <div>
            <h3 class="text-white font-heading font-semibold text-sm uppercase tracking-wider mb-4">
              Contacto
            </h3>
            <ul class="space-y-3 text-sm">
              <li class="flex items-center gap-2">
                <svg class="w-4 h-4 text-accent-500 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
                </svg>
                <a href={`tel:${siteConfig.phone}`} class="text-surface-300 hover:text-accent-400 transition-colors">
                  {siteConfig.phoneDisplay}
                </a>
              </li>
              <li class="flex items-center gap-2">
                <svg class="w-4 h-4 text-accent-500 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                </svg>
                <a href={`mailto:${siteConfig.email}`} class="text-surface-300 hover:text-accent-400 transition-colors break-all">
                  {siteConfig.email}
                </a>
              </li>
              <li class="flex items-center gap-2">
                <svg class="w-4 h-4 text-green-500 shrink-0" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z" />
                </svg>
                <a href={waUrl} target="_blank" rel="noopener" class="text-surface-300 hover:text-green-400 transition-colors">
                  WhatsApp
                </a>
              </li>
            </ul>
          </div>

          <!-- Redes sociales (solo si hay al menos una) -->
          {activeSocials.length > 0 && (
            <div>
              <h3 class="text-white font-heading font-semibold text-sm uppercase tracking-wider mb-4">
                Síguenos
              </h3>
              <div class="flex gap-3">
                {siteConfig.social.instagram && (
                  <a href={siteConfig.social.instagram} target="_blank" rel="noopener noreferrer"
                    class="w-10 h-10 rounded-lg flex items-center justify-center text-surface-300 hover:text-accent-400 transition-colors"
                    style="background: var(--color-primary-800);"
                    aria-label="Instagram"
                  >
                    <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                      <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 100 12.324 6.162 6.162 0 000-12.324zM12 16a4 4 0 110-8 4 4 0 010 8zm6.406-11.845a1.44 1.44 0 100 2.881 1.44 1.44 0 000-2.881z" />
                    </svg>
                  </a>
                )}
                {siteConfig.social.facebook && (
                  <a href={siteConfig.social.facebook} target="_blank" rel="noopener noreferrer"
                    class="w-10 h-10 rounded-lg flex items-center justify-center text-surface-300 hover:text-accent-400 transition-colors"
                    style="background: var(--color-primary-800);"
                    aria-label="Facebook"
                  >
                    <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                      <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z" />
                    </svg>
                  </a>
                )}
              </div>
            </div>
          )}
        </div>

        <!-- Bottom bar -->
        <div class="mt-12 pt-8 border-t border-surface-800 flex flex-col sm:flex-row justify-between items-center gap-3">
          <p class="text-xs text-surface-400">
            &copy; {new Date().getFullYear()} {siteConfig.name}.
            Todos los derechos reservados.
          </p>
          <p class="text-xs text-surface-400">
            Desarrollado por
            <a
              href="https://dataimpulsa.cl"
              target="_blank"
              rel="noopener noreferrer"
              class="text-accent-400 hover:text-accent-300 transition-colors"
            >
              DatAImpulsa
            </a>
          </p>
        </div>
      </div>
    </footer>

    <!-- ========== BOTÓN WHATSAPP FLOTANTE ========== -->
    <a
      href={waUrl}
      target="_blank"
      rel="noopener noreferrer"
      class="fixed bottom-6 right-6 z-40 w-14 h-14 bg-green-500 hover:bg-green-600
             rounded-full flex items-center justify-center shadow-lg
             hover:scale-110 transition-all duration-300"
      aria-label="Contactar por WhatsApp"
    >
      <svg class="w-7 h-7 text-white" fill="currentColor" viewBox="0 0 24 24">
        <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z" />
      </svg>
    </a>

    <!-- ========== SCRIPTS ========== -->
    <script>
      // --- Theme Toggle ---
      function toggleTheme() {
        const html = document.documentElement;
        const isDark = html.classList.toggle('dark');
        localStorage.setItem('theme', isDark ? 'dark' : 'light');
      }

      document.getElementById('theme-toggle')?.addEventListener('click', toggleTheme);
      document.getElementById('theme-toggle-mobile')?.addEventListener('click', toggleTheme);

      // --- Mobile Menu ---
      const menuBtn = document.getElementById('mobile-menu-btn');
      const mobileMenu = document.getElementById('mobile-menu');
      const iconOpen = document.getElementById('menu-icon-open');
      const iconClose = document.getElementById('menu-icon-close');

      menuBtn?.addEventListener('click', () => {
        const isOpen = mobileMenu?.classList.contains('max-h-0');

        if (isOpen) {
          mobileMenu?.classList.remove('max-h-0', 'hidden');
          mobileMenu?.classList.add('max-h-96');
          iconOpen?.classList.add('hidden');
          iconClose?.classList.remove('hidden');
          menuBtn.setAttribute('aria-expanded', 'true');
        } else {
          mobileMenu?.classList.add('max-h-0');
          mobileMenu?.classList.remove('max-h-96');
          iconOpen?.classList.remove('hidden');
          iconClose?.classList.add('hidden');
          menuBtn.setAttribute('aria-expanded', 'false');
          setTimeout(() => mobileMenu?.classList.add('hidden'), 300);
        }
      });

      // Cerrar menú al hacer click en un link
      mobileMenu?.querySelectorAll('a').forEach((link) => {
        link.addEventListener('click', () => {
          mobileMenu?.classList.add('max-h-0');
          mobileMenu?.classList.remove('max-h-96');
          iconOpen?.classList.remove('hidden');
          iconClose?.classList.add('hidden');
          menuBtn?.setAttribute('aria-expanded', 'false');
          setTimeout(() => mobileMenu?.classList.add('hidden'), 300);
        });
      });

      // --- Header: transparent on hero, solid on scroll ---
      const header = document.getElementById('site-header');

      header?.classList.add('at-top');

      window.addEventListener('scroll', () => {
        const currentScroll = window.scrollY;

        if (currentScroll > 80) {
          header?.classList.remove('at-top');
          header?.classList.add('shadow-md');
          if (header) {
            header.style.backgroundColor = 'var(--bg-nav)';
            header.style.setProperty('backdrop-filter', 'blur(14px)');
            header.style.setProperty('-webkit-backdrop-filter', 'blur(14px)');
          }
        } else {
          header?.classList.add('at-top');
          header?.classList.remove('shadow-md');
          if (header) {
            header.style.backgroundColor = 'transparent';
            header.style.setProperty('backdrop-filter', 'none');
            header.style.setProperty('-webkit-backdrop-filter', 'none');
          }
        }

      }, { passive: true });

      // --- Scroll Reveal (IntersectionObserver) ---
      const revealElements = document.querySelectorAll('.reveal, .reveal-stagger');

      if (revealElements.length > 0) {
        const observer = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                entry.target.classList.add('is-visible');
                observer.unobserve(entry.target);
              }
            });
          },
          {
            threshold: 0.15,
            rootMargin: '0px 0px -40px 0px',
          }
        );

        revealElements.forEach((el) => observer.observe(el));
      }
    </script>

    <style is:global>
      /* Nav link styles */
      .nav-link {
        position: relative;
        font-size: 0.875rem;
        font-weight: 500;
        color: var(--text-body);
        transition: color 0.2s ease;
      }

      /* White nav links when header is transparent over hero */
      #site-header.at-top .nav-link { color: rgba(255, 255, 255, 0.85); }
      #site-header.at-top .nav-link:hover { color: white; }
      #site-header.at-top .logo-name { color: white !important; }
      #site-header.at-top .logo-sub { color: rgba(255, 255, 255, 0.6) !important; }
      #site-header.at-top .theme-toggle-icon { color: rgba(255, 255, 255, 0.7) !important; }
      #site-header.at-top .burger-icon { color: white !important; }

      .nav-link:hover {
        color: var(--color-primary-600);
      }

      :root.dark .nav-link:hover {
        color: var(--color-accent-400);
      }

      .nav-link::after {
        content: '';
        position: absolute;
        bottom: -4px;
        left: 0;
        width: 0;
        height: 2px;
        background: var(--color-accent-500);
        transition: width 0.3s var(--ease-out-expo);
      }

      .nav-link:hover::after {
        width: 100%;
      }

      /* Mobile nav link */
      .mobile-nav-link {
        display: block;
        padding: 0.625rem 1rem;
        font-size: 0.9375rem;
        font-weight: 500;
        color: var(--text-body);
        border-radius: 0.5rem;
        transition: background-color 0.2s ease, color 0.2s ease;
      }

      .mobile-nav-link:hover {
        background-color: var(--bg-card-hover);
        color: var(--color-primary-600);
      }

      :root.dark .mobile-nav-link:hover {
        color: var(--color-accent-400);
      }

      /* Primary button */
      .btn-primary {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.625rem 1.5rem;
        font-size: 0.875rem;
        font-weight: 600;
        color: white;
        background-color: var(--color-primary-600);
        border-radius: 0.5rem;
        transition: all 0.2s ease;
        box-shadow: 0 1px 3px rgba(30, 45, 77, 0.2);
      }

      .btn-primary:hover {
        background-color: var(--color-primary-700);
        color: white;
        transform: translateY(-1px);
        box-shadow: 0 4px 12px rgba(30, 45, 77, 0.3);
      }

      /* Secondary button */
      .btn-secondary {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.625rem 1.5rem;
        font-size: 0.875rem;
        font-weight: 600;
        color: var(--color-primary-700);
        background-color: transparent;
        border: 2px solid var(--color-primary-300);
        border-radius: 0.5rem;
        transition: all 0.2s ease;
      }

      .btn-secondary:hover {
        background-color: var(--color-primary-600);
        border-color: var(--color-primary-600);
        color: white;
        transform: translateY(-1px);
      }

      :root.dark .btn-secondary {
        color: var(--color-accent-400);
        border-color: var(--color-accent-700);
      }

      :root.dark .btn-secondary:hover {
        background-color: var(--color-accent-600);
        border-color: var(--color-accent-600);
        color: white;
      }

      /* Section title utility */
      .section-title {
        font-family: var(--font-heading);
        font-size: clamp(1.75rem, 4vw, 2.5rem);
        font-weight: 800;
        color: var(--text-heading);
        line-height: 1.15;
      }

      .section-subtitle {
        font-size: 1.0625rem;
        color: var(--text-muted);
        max-width: 40rem;
        line-height: 1.7;
      }
    </style>
  </body>
</html>